{
  "name": "shopzone-cypress",
  "version": "1.0.0",
  "description": "Cypress E2E Tests for ShopZone",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:headed": "cypress run --headed",
    "cy:report": "npx mochawesome-merge cypress/reports/*.json > cypress/reports/combined.json && npx marge cypress/reports/combined.json -f report -o cypress/reports/html"
  },
  "devDependencies": {
    "cypress": "^13.6.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
